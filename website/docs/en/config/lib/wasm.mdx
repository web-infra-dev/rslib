---
description: 'Configure lib.wasm to control how Rslib handles `.wasm` imports in the output.'
---

# lib.wasm

- **Type:**

```ts
type Wasm = {
  mode?: 'compile' | 'preserve';
};
```

- **Default:** `undefined`

Configure how Rslib handles `.wasm` modules used with the [WebAssembly ESM Integration](https://github.com/WebAssembly/esm-integration) proposal syntax.

See [WebAssembly](/guide/advanced/webassembly) for more details.

## wasm.mode

- **Type:** `'compile' | 'preserve'`
- **Default:** `'compile'` when [bundle](/config/lib/bundle) is `true`, `'preserve'` when `bundle` is `false`

Controls how `.wasm` modules are emitted.

- `'compile'`: Rspack parses the `.wasm` module and generates JavaScript glue code and runtime loading logic. Suitable when the library is consumed directly by a runtime such as Node.js or the browser.
- `'preserve'`: Rslib keeps the `.wasm` import in the output JavaScript and emits the binary as-is. This mode requires [bundle](/config/lib/bundle) to be `false` and is suitable when the library is consumed by another build tool or a runtime that supports WebAssembly ESM Integration natively.

In preserve mode, the `.wasm` file keeps its source-relative path and original filename. [output.distPath.wasm](/config/rsbuild/output#outputdistpath) and [output.filenameHash](/config/rsbuild/output#outputfilenamehash) do not affect `.wasm` files in preserve mode.

```ts title="rslib.config.ts"
export default {
  lib: [
    {
      bundle: false,
      format: 'esm',
      wasm: {
        mode: 'preserve',
      },
    },
  ],
};
```

:::note

`wasm` can only be configured when [format](/config/lib/format) is `'esm'` (the default value). In addition, `wasm.mode: 'preserve'` requires [bundle](/config/lib/bundle) to be `false`. The build will fail for unsupported combinations.

:::

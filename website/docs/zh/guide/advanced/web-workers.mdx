---
description: '本文将介绍在 Rslib 项目中如何配置和使用 Web Workers。'
---

import { Tabs, Tab } from '@theme';

# Web Workers

Rslib 支持 Web Worker 模块。推荐使用标准的 `new Worker(new URL(..., import.meta.url))` 语法。你也可以使用 `?worker` 和 `?worker&inline` query 后缀导入 Worker。

:::note

使用 Web Worker 时，仅支持生成 ESM 格式的产物，因此 [format](/config/lib/format) 必须设置为 `'esm'`（默认值）。

:::

## 使用 Worker 构造器

### 基本用法

使用标准构造器语法创建 Worker：

```ts
new Worker(new URL('./worker.ts', import.meta.url));
```

Rslib 会重写 Worker URL，使其指向生成的 JavaScript 文件，并将 Worker 输出为 ES 模块：

```js
new Worker(new URL('./worker.js', import.meta.url), {
  type: 'module',
});
```

### 限制

该语法的处理依赖静态分析，因此 `new URL(..., import.meta.url)` 表达式需要保持内联，不支持传入字符串或变量：

```ts
// 不支持
new Worker('./worker.ts');

// 不支持
const workerUrl = new URL('./worker.ts', import.meta.url);
new Worker(workerUrl);
```

更多 Worker 语法及其限制，请参考 [Rspack - Web Workers](https://rspack.rs/zh/guide/features/web-workers)。

### 产物模式

Worker 构造器语法可用于 [bundle 和 bundleless 模式](/guide/basic/output-structure#bundle--bundleless)，下面通过一组示例源码展示两种模式下的构建结果：

<Tabs>
<Tab label="src/index.ts">

```ts
export const worker = new Worker(new URL('./worker.ts', import.meta.url));
```

</Tab>
<Tab label="src/worker.ts">

```ts
import { add } from './helper';

self.onmessage = ({ data }: MessageEvent<[number, number]>) => {
  self.postMessage(add(data[0], data[1]));
};
```

</Tab>
<Tab label="src/helper.ts">

```ts
export const add = (left: number, right: number) => left + right;
```

</Tab>
</Tabs>

#### Bundle 模式

在 bundle 模式下，Rslib 会将 Worker 入口拆分为独立 chunk，并将 Worker 导入的模块打包到该 chunk 中：

<Tabs>
<Tab label="dist/index.js">

```js
const worker = new Worker(new URL('./worker.js', import.meta.url), {
  type: 'module',
});
export { worker };
```

</Tab>
<Tab label="dist/worker.js">

```js
const add = (left, right) => left + right;
self.onmessage = ({ data }) => {
  self.postMessage(add(data[0], data[1]));
};
export {};
```

</Tab>
</Tabs>

#### Bundleless 模式

在 bundleless 模式下，Rslib 会分别转换每个源模块，并将 Worker URL 和 Worker 内部的导入重写为对应的 ESM 产物路径：

<Tabs>
<Tab label="dist/index.js">

```js
const worker = new Worker(new URL('./worker.js', import.meta.url), {
  type: 'module',
});
export { worker };
```

</Tab>
<Tab label="dist/worker.js">

```js
import { add } from './helper.js';
self.onmessage = ({ data }) => {
  self.postMessage(add(data[0], data[1]));
};
```

</Tab>
<Tab label="dist/helper.js">

```js
const add = (left, right) => left + right;
export { add };
```

</Tab>
</Tabs>

## 使用 Query 后缀导入

Rslib 还支持 [Rsbuild 提供的 query 后缀语法](https://rsbuild.rs/zh/guide/basic/web-workers#使用-query-后缀导入)。

:::note

Query 后缀仅支持 bundle 模式。

:::

### 输出独立 Worker 文件

在 Worker 导入路径后添加 `?worker`：

```ts
import MyWorker from './worker.ts?worker';

export const worker = new MyWorker();
```

Rslib 会将 Worker 输出为独立的 ESM 文件。

### 内联 Worker

添加 `?worker&inline` 可以内联 Worker：

```ts
import MyWorker from './worker.ts?worker&inline';

export const worker = new MyWorker();
```

Rslib 会将 Worker 代码编码为 data URL，并内联到导入该 Worker 的 JavaScript 文件中，不再输出独立的 Worker 文件。

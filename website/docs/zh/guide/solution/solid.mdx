---
description: '使用 Rslib 构建 Solid 组件库，涵盖编译输出与源码 JSX 双输出、"solid" 导出条件与类型声明生成。'
---

# Solid

在本文档中，你将学习如何使用 Rslib 构建 Solid 组件库，你可在 [示例](https://github.com/rstackjs/rstack-examples/tree/main/rslib) 中查看 Solid 相关演示项目。

## 创建 Solid 项目

你可以使用 `create-rslib` 创建 Rslib + Solid 项目。只需执行以下命令：

import { PackageManagerTabs } from '@theme';

<PackageManagerTabs
  command={{
    npm: 'npm create rslib@latest',
    yarn: 'yarn create rslib',
    pnpm: 'pnpm create rslib@latest',
    bun: 'bun create rslib@latest',
  }}
/>

然后，当提示 "Select template" 时选择 `Solid`。

## 在现有 Rslib 项目中使用

开发 Solid 组件库，需要在 `rslib.config.ts` 中设置 [target](/config/rsbuild/output#outputtarget) 为 `"web"`。这一点至关重要，因为 Rslib 默认将 `target` 设置为 `"node"`。

要编译 Solid JSX，你需要同时注册 [@rsbuild/plugin-babel](https://rsbuild.rs/zh/plugins/list/plugin-babel) 和 [@rsbuild/plugin-solid](https://rsbuild.rs/zh/plugins/list/plugin-solid) 插件。Solid 插件使用 `babel-preset-solid` 将 JSX 转换为 Solid 的运行时 DOM 表达式。

```ts title="rslib.config.ts"
import { pluginBabel } from '@rsbuild/plugin-babel';
import { pluginSolid } from '@rsbuild/plugin-solid';
import { defineConfig } from '@rslib/core';

export default defineConfig({
  source: {
    entry: {
      index: ['./src/**'],
    },
  },
  lib: [
    {
      bundle: false,
      dts: true,
      format: 'esm',
      plugins: [
        pluginBabel({
          include: /\.(?:jsx|tsx)$/,
        }),
        pluginSolid(),
      ],
    },
  ],
  output: {
    target: 'web',
  },
});
```

## 双输出与 "solid" 导出条件

Solid 组件库通常同时提供**编译后的输出**和**保留 JSX 的源码输出**。`package.json` 中的 `"solid"` 导出条件指向未编译的 JSX 源码，允许下游打包器（如配置了 `@rsbuild/plugin-solid` 的项目）以最优的方式本地编译（DOM vs SSR）。

添加第二个 lib 配置来保留 JSX：

```ts title="rslib.config.ts"
import { pluginBabel } from '@rsbuild/plugin-babel';
import { pluginSolid } from '@rsbuild/plugin-solid';
import { defineConfig } from '@rslib/core';

export default defineConfig({
  source: {
    entry: {
      index: ['./src/**'],
    },
  },
  lib: [
    {
      bundle: false,
      dts: true,
      format: 'esm',
      plugins: [
        pluginBabel({
          include: /\.(?:jsx|tsx)$/,
        }),
        pluginSolid(),
      ],
    },
    {
      bundle: false,
      format: 'esm',
      output: {
        distPath: {
          root: './dist/source',
        },
        filename: {
          js: '[name].jsx',
        },
      },
    },
  ],
  output: {
    target: 'web',
  },
});
```

然后在 `package.json` 中配置 exports：

```json title="package.json"
{
  "exports": {
    ".": {
      "solid": "./dist/source/index.jsx",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  }
}
```

## TypeScript

对于使用 TypeScript 的 Solid 项目，在 `tsconfig.json` 中设置 `"jsx": "preserve"` 和 `"jsxImportSource": "solid-js"`：

```json title="tsconfig.json"
{
  "compilerOptions": {
    "jsx": "preserve",
    "jsxImportSource": "solid-js"
  }
}
```

## SSR

要为服务端渲染构建 Solid 库，可通过 `pluginSolid()` 传入 `solid` 选项：

```ts
pluginSolid({
  solid: {
    generate: 'ssr',
    hydratable: true,
  },
})
```

## 进一步了解

- [Rsbuild Solid 插件](https://rsbuild.rs/zh/plugins/list/plugin-solid)
- [babel-preset-solid](https://github.com/solidjs/solid/tree/main/packages/babel-preset-solid)
